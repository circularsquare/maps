data/
cache/
venv/
*.gpkg
*.pkl
*.geojson
helper1m/countries/
flights/pull/
__pycache__/
*.pyc
*.stackdump
**/.git_bak/
*.env
secrets.*

# Large intermediate / regenerable products — kept on disk, not uploaded.
# (New map outputs elsewhere are still tracked by default.)
# NetworkX graph intermediates:
*.gpickle
# raw / intermediate rider data (regenerable from fetch):
riders/londonriders/trains.json
riders/nycriders/trains.json
riders/nycriders/data.json
# street-render experiments / colormap variants:
nystreets/osm*.png
nystreets/nystreets.png
# poster renders + baked dot arrays, regenerable from bake/render/insets:
posters/*/build/
# hand-edit backup written by posters/nycriders/nudge/serve.py on each save:
posters/*/nudge/nudges.backup.json
